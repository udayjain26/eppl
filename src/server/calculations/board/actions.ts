'use server'

import { BoardCostData } from '@/app/estimates/[id]/_components/calculation-components/board-calculation'

export async function calculateBoardCost(): Promise<BoardCostData | undefined> {
  return undefined
}
